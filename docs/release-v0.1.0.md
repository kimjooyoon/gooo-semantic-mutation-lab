# v0.1.0 release record

This file records the immutable release identity after the merge commit is
known. The release tag is created on `main`, never moved, and the release
assets include the source archive checksums and the CI conformance report.

The final tag commit and asset digests are published in the GitHub release
notes and are the authoritative values for this record.
