# Repository ownership

This repository is owned by Codex task
`01a0558a-d2a9-7e42-8bae-3f5e34155c29` for the semantic mutation lab scope.

The owned scope is the complete repository: the `.gooo` source declaration,
semantic IR compiler, caller-owned mutant generation, evaluator, exact-count
report, conformance fixtures, CI workflow, pull request, and v0.1.0 release.

Other tasks must not modify this repository while the owner is active. The
owner must stop immediately and report the exact changed paths if an
unexpected change appears after this receipt is recorded.
